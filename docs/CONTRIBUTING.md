# Contributing

## Setup

```bash
just setup
```

Requires [`just`](https://github.com/casey/just) (`cargo install just`).

## Testing

```bash
just test      # 28 unit + 19 integration
just perf      # criterion benchmarks
```
